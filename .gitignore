.env
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
.DS_Store
logs/
*.log

# IDE / tool caches
.cascade/
.hypothesis/
.benchmarks/

# Generated HTML files (test outputs)
*.html

# Encryption key
data/.encryption_key

# Database files (contain sensitive data)
trading.db
data/*.db

# ML model artifacts (regenerated on train)
data/*.pkl

# Large market data CSVs (50-60MB each) — stored as GitHub Actions artifacts,
# NOT in git. Regenerate locally: python3 scripts/fetch_historical_data.py
data/training_data.csv
data/extended_historical_data.csv

# Artifacts directories (generated per run)
artifacts/

# Backtest output (regenerated per run)
backtest_results/
