# Runtime dependencies for the trading platform (GitHub Actions + SQLite).
# Slimmed 2026-06-12: removed a ghost architecture (fastapi/uvicorn/sqlalchemy/
# alembic/psycopg2/asyncpg/redis/bcrypt/flask/streamlit/plotly/jwt/...) that
# was never imported anywhere — it slowed every CI and trading run and
# inflated the supply-chain surface. Audit: grep -rl "import X|from X" src/ scripts/ tests/

# Broker + data
alpaca-py==0.11.0
yfinance==0.2.54  # secondary equity data source when Alpha Vantage is unavailable
requests==2.33.0

# Core numerics
pandas==2.0.3
numpy==1.24.4
scikit-learn==1.5.2  # ML Momentum fallback model (strategy disabled; code retained one quarter)

# Validation / config
pydantic>=2,<3  # src/validation/schemas.py (v2 API); also an alpaca-py dependency
PyYAML==6.0.1
python-dotenv==1.0.0

# Reporting
matplotlib==3.9.4  # email charts
vaderSentiment==3.3.2  # News Sentiment scoring (strategy disabled; code retained one quarter)
# Optional: enables LLM-summarized news digest. Falls back to heuristic
# summarizer if OPENAI_API_KEY is unset, so this dep is safe to install
# even on environments without an API key.
openai>=1.40.0,<2.0.0

# Time handling
pytz==2024.1
python-dateutil==2.8.2

# Tests
pytest==7.4.3
pytest-timeout==2.2.0
