# Development and testing dependencies — NOT needed for production trading.
# Install with: pip install -r requirements-dev.txt
# The GHA CI workflow installs these explicitly; production GHA runs do not need them.
-r requirements.txt
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-timeout==2.2.0
pytest-cov
pre-commit
