[pytest]
addopts = -v --tb=short --no-header -m "not slow"
testpaths = tests/unit tests/component
timeout = 30
markers =
    slow: tests that load real data files or are computationally expensive (run with: pytest -m slow)
