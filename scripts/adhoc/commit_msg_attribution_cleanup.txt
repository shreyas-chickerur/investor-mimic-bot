feat(observability): per-strategy attribution + adhoc cleanup

Strategy attribution (user request: "measure which stocks/transactions
were triggered by which strategies so we can analyze"):
  scripts/strategy_attribution.py (NEW)
    Reusable report listing per-strategy aggregates (trade count,
    buys/sells, notional, realized P&L), open positions grouped by
    strategy, and any cross-strategy symbol overlap. Supports --csv
    for spreadsheet analysis and --days N for windowed views.
  scripts/generate_daily_email.py
    Open-positions section now displays the strategy badge under each
    holding (already shown on trade rows). Same _short_strategy()
    label and STRAT_BRAND palette used elsewhere — visually consistent.

SELL intent persistence root cause (audit issue #3, deferred earlier):
  Confirmed via scripts/adhoc/sell_intent_rootcause.sql that all 19
  pre-existing SELL trades without order_intents rows predate commit
  70ccb62 (2026-05-27 02:42 UTC) which introduced the SELL intent
  path. Zero post-fix SELLs exist yet, so the forward-going path is
  clean — no code change needed, gap is purely historical.

Workflow lint warnings (audit cleanup item):
  Verified via `gh secret list` that OPENAI_API_KEY and
  HEALTHCHECKS_URL secrets are intentionally absent. Both are optional:
  - OPENAI_API_KEY: enables LLM news summaries; falls back to heuristic
  - HEALTHCHECKS_URL: external dead-man's switch; gated by `if [ -n ]`
  Lint warnings are false positives and can be ignored or silenced via
  the workflow editor's per-line ignore.

Adhoc cleanup (kept reusable, deleted throwaways):
  Kept (5):  backfill_phantom_cleanup.py, verify_against_alpaca.py,
             clean_data.py, pre_flight_check.py, serve_dashboard.py
  Deleted (18): close_xlv_orphan.py, all commit_msg_*.txt, issue_sweep.{sql,out},
                phantom_intent_status.{sql,out}, probe_allocator.py,
                sell_intent_audit.{sql,out}, sell_intent_rootcause.sql,
                trade_intent_audit.{sql,out}, verify_against_alpaca.out,
                xlv_forensics.{sql,out}

Tests: 354 passed, 1 skipped.
