docs(audit): broker-verified bug list — phantom positions are root cause

User noted Claude Code addressed XLV; I queried Alpaca directly via
scripts/adhoc/verify_against_alpaca.py to ground-truth all open issues.

Key broker findings:

- Account healthy: $94k cash, $96k equity, $190k buying power, ACTIVE.
- Broker holds ONLY 11 XLV. Three local positions (ABBV 5 sh, AMD 2 sh,
  AMZN 4 sh) DON'T EXIST AT THE BROKER. They are phantom — their
  creating BUY orders all expired/canceled at Alpaca but the bot's
  optimistic position records were never cleaned up.
- 17 of 19 local SELL trades are real broker fills (normal slippage).
  Only XLV 5/21 and TSLA 5/18 are phantom SELLs.
- The XLV +$24 P&L is fictitious (broker canceled the 5/21 SELL),
  but Claude Code submitted a fresh SELL today (5/28 22:23, accepted)
  which will fill at next open and close the position cleanly.
- 6 of the 7 "stale ACKED" intents I flagged are actually canceled at
  broker (caused resubmissions). The 7th (XLV BUY) is filled. 10
  additional older ACKEDs are also actually filled — local status
  drift in both directions.

Final consolidated bug list (§17) in priority order:
1. Phantom position cleanup (NEW root cause) — ABBV/AMD/AMZN don't
   exist at broker. Need reverse-position-update when BUY intent gets
   canceled/expired.
2. ACKED↔FILLED status drift — verify_order_statuses only walks
   current-run trades, leaves old intents stale.
3. SELL trades bypass order_intents — 0 SELL intents persisted despite
   19 SELL trades.
4. News Sentiment naive time-exit (code bug, currently moot since
   positions are phantom).
5. Capital allocation observability — DB column equal-weighted while
   sizing is Sharpe-weighted.
6-7. Tax-aware exit + OPG timing unit test failures.
8. Email script shares > 0 / capital_allocation filters.
9. Cross-strategy AMD overlap — design question.
10-11. cron-job.org timing buffer + Python 3.9 test compat.

Adhoc artifact: scripts/adhoc/verify_against_alpaca.py reproduces the
full broker reconciliation. Useful for verifying any future suspected
discrepancies.
